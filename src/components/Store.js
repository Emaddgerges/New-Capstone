import React from 'react'

const Store = () => {
    {/*  const [products, setProducts] = useState([])
    useEffect(() => {
            fetch('https://fakestoreapi.com/products')
            .then((response) => response.json())
            .then((data) => setProducts(data))
      }, []);
    
          if (products.length ===0) {
        return <div>loding players...</div>;
      }  
      
  return (     
  <>
  
    {products.map((product) => (
        <Col key={product.id}>
        <div className="ULALLPLAYERS" key={product.id}>
          <div className="product.title">NAME: {product.title}</div>
          <div>price: {product.price}</div>
          <img src={product.image} className="img" alt="#"/>
          <div><button onClick={(event) => {
    const clickedplayers = product.find(({id})=>id===parseInt(event.target.id));
  }}
    >See Details</button> </div>
        </div></Col>
      )  )}

    <div>Store</div>
    </>
  )*/}
}

export default Store;